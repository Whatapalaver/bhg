<%= form_with(model: [@game, game_hand], local: true) do |form| %>
  <% if game_hand.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game_hand.errors.count, "error") %> prohibited this game_hand from being saved:</h2>

      <ul>
      <% game_hand.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :game_hand %>
    <%= form.select :direction, GameHand.directions.keys %>
  </div>

  <div class="field">
    <%= form.label :cards %>
    <%= form.text_field :cards %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>